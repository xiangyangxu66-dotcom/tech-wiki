[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts = -v --tb=short --strict-markers --no-header -p no:warnings
testpaths = tests

# Coverage (run via: pytest --cov=wiki --cov-report=term-missing)
# Not in addopts by default — on-demand only to keep test runs fast
